services:
  web:
    build: .
    ports:
      - "5000:80"
    # depends_on:
    #   db:
    #     condition: service_healthy
    env_file:
      - ./.env
    volumes:
      - .:/app

    
  
  worker:
    build: .
    container_name: rq_worker
    command: rq worker --url $REDIS_URL emails
    env_file:
      - ./.env
#   db:
#     image: postgres:16
#     environment:
#       - POSTGRES_PASSWORD=password
#       - POSTGRES_DB=myapp
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#     healthcheck:
#       test: pg_isready -d $${POSTGRES_DB} -U postgres
#       interval: 2s
#       retries: 10
#     ports:
#       - "5432:5432"
# volumes:
#   postgres_data: